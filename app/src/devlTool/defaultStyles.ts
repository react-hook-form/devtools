import colors from './colors';

const defaultStyles = {
  button: {
    appearance: 'none' as any,
    margin: 0,
    border: 0,
    color: 'white',
    padding: 5,
    borderRadius: 0,
    background: colors.blue,
  },
};

export default defaultStyles;
